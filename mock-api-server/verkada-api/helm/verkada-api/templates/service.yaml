apiVersion: v1
kind: Service
metadata:
  name: {{ include "verkada-api.fullname" . }}
  labels:
    {{- include "verkada-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "verkada-api.selectorLabels" . | nindent 4 }}
